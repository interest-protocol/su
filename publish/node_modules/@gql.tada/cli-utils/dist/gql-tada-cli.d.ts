interface GenerateSchemaOptions {
  headers?: Record<string, string>;
  output?: string;
  cwd?: string;
}
declare function generateSchema(
  target: string,
  { headers, output, cwd }: GenerateSchemaOptions
): Promise<void>;
declare function generateTadaTypes(shouldPreprocess?: boolean, cwd?: string): Promise<void>;
declare function main(): Promise<void>;

export { main as default, generateSchema, generateTadaTypes };
